# Server-side code — deployed separately on Railway, not Hostinger
webhook-server/

# Internal tools and GHL config — not for public_html
tools/
ghl-config/

# Misc
*.log
.DS_Store
Thumbs.db
desktop.ini

# Ops dashboard — not for public host (exposes admin view). Serve locally / behind auth.
dashboard.html

# Personal-brand site — separate from Willis Advocacy Group; must not deploy to this domain.
uhiawillis-site/
